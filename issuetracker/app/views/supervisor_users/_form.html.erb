<%= form_with(model: supervisor_user) do |form| %>
  <% if supervisor_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(supervisor_user.errors.count, "error") %> prohibited this supervisor_user from being saved:</h2>

      <ul>
        <% supervisor_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
