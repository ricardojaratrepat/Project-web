<%= form_with(model: executive_user) do |form| %>
  <% if executive_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(executive_user.errors.count, "error") %> prohibited this executive_user from being saved:</h2>

      <ul>
        <% executive_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
