<%= form_with(model: support_staff) do |form| %>
  <% if support_staff.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(support_staff.errors.count, "error") %> prohibited this support_staff from being saved:</h2>

      <ul>
        <% support_staff.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
